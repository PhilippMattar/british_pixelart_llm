"""Instruction prompts for teacher generation.

`spike.py` is a small hand-written set for the humor spike. The full run (§6.2) samples ~2.5k
from open instruction sets (Dolly, Alpaca-cleaned, OpenAssistant) plus hand-written trigger
prompts — built by prompts/build_bank.py (added when we scale past the spike).
"""
